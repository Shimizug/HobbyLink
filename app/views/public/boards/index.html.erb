<div class='container'>
  <div class="row">
    <div class= "col-6">
      <h2>掲示板一覧</h2>
    </div>
    <div class="col-2 offset-4">
      <% unless current_member.email == "guest@example.com" %>
        <%= link_to "新規作成へ", new_board_path, class: "btn btn-info" %>
      <% end %>
    </div>
    <%= render 'index', boards: @boards %>
  </div>
</div>
<div class="offset-5 mb-5">
  <%= paginate @boards %>
</div>

