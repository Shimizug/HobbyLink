<table class="table table-bordered">
  <thead>
    <tr>
      <th class="text-nowrap">ジャンル名</th>
      <th>ジャンル概要</th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
    <tr>
      <td><%= genre.name %></td>
      <td><%= genre.body %></td>
    </tr>
    <% end %>
  </tbody>
</table>

