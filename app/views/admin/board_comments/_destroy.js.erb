$('.board-comments-index').html("<%= j(render partial: "admin
/board_comments/index", locals: {board: @board, board_comments: @board_comments}) %>")
$('.comment-paginate').html("<%= j(paginate @board_comments, params: {controller: 'boards', action: 'show', id: @board.id}) %>")